import React from "react";
import styled from "styled-components";

const StyledActiveListening = styled.div``;

export function ActiveListening() {
  return <StyledActiveListening>Active Listening</StyledActiveListening>;
}
