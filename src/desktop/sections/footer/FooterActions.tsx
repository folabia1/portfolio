import React from "react";
import styled from "styled-components";

const StyledFooterActions = styled.div`
  align-self: end;
`;

export function FooterActions() {
  return <StyledFooterActions>Footer Actions</StyledFooterActions>;
}
